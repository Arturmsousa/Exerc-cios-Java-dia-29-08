import java.util.Scanner;

/**
 * Verificação de Idade e Acesso
 * Objetivo: Praticar tipos de dados, variáveis, comandos condicionais,
 * operadores relacionais, entrada de dados e laços de repetição
 */
public class VerificacaoIdade {
    
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        System.out.println("=== SISTEMA DE VERIFICAÇÃO DE IDADE ===");
        System.out.println("Para acessar o conteúdo restrito, você deve ter 18 anos ou mais.");
        System.out.println();
        
        // Declaração de variáveis
        int idade = -1; // Inicializa com valor inválido para entrar no laço
        boolean idadeValida = false;
        
        // Laço de repetição para garantir entrada de idade válida
        while (!idadeValida) {
            System.out.print("Digite sua idade: ");
            
            try {
                // Entrada de dados - solicita a idade
                idade = scanner.nextInt();
                
                // Validação se a idade é um número positivo
                if (idade < 0) {
                    System.out.println("ERRO: A idade deve ser um número positivo!");
                    System.out.println("Tente novamente.\n");
                } else if (idade == 0) {
                    System.out.println("ERRO: A idade deve ser maior que zero!");
                    System.out.println("Tente novamente.\n");
                } else {
                    // Idade válida - sai do laço
                    idadeValida = true;
                }
                
            } catch (Exception e) {
                System.out.println("ERRO: Por favor, digite apenas números inteiros!");
                System.out.println("Tente novamente.\n");
                // Limpa o buffer do scanner
                scanner.nextLine();
            }
        }
        
        System.out.println(); // Linha em branco para melhor formatação
        
        // Comandos condicionais com operadores relacionais
        if (idade < 18) {
            System.out.println("🚫 ACESSO NEGADO");
            System.out.println("Você precisa ter pelo menos 18 anos para acessar este conteúdo.");
            
            // Calcula quantos anos faltam
            int anosFaltantes = 18 - idade;
            if (anosFaltantes == 1) {
                System.out.println("Falta apenas " + anosFaltantes + " ano para você ter acesso.");
            } else {
                System.out.println("Faltam " + anosFaltantes + " anos para você ter acesso.");
            }
            
        } else {
            System.out.println("✅ ACESSO PERMITIDO");
            System.out.println("Bem-vindo! Você pode acessar o conteúdo restrito.");
            
            // Mensagem adicional baseada na faixa etária
            if (idade >= 18 && idade <= 25) {
                System.out.println("Categoria: Jovem adulto");
            } else if (idade >= 26 && idade <= 59) {
                System.out.println("Categoria: Adulto");
            } else if (idade >= 60) {
                System.out.println("Categoria: Idoso");
            }
        }
        
        System.out.println("\nIdade informada: " + idade + " anos");
        System.out.println("Obrigado por usar nosso sistema!");
        
        scanner.close();
    }
}
